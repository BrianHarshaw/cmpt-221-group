Here contains the create statements in SQL for the tables. This folder will also contain the values and information that needs to be put into each table. 
Tables include: User, Asset, Department, and Permission
